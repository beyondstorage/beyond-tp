package task

const (
	// NotificationHello will be sent from client to server in first the contact.
	ClientHello = "hello"

	// ServerTaskAvailable will be sent from server to client to notify new task available.
	ServerTaskAvailable = "task available"
)
