import * as React from 'react';
import * as ReactDOM from 'react-dom';


import '../scss/index.scss';

// moment.locale(Cookies.get('lang') || 'en');

ReactDOM.render(
  <div>
    DM client page
  </div>,
  document.getElementById('root'),
);