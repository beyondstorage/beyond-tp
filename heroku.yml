build:
  docker:
    beyondtp: Dockerfile
run:
  server: ./bin/beyondtp server --db db --host localhost --port "$PORT" --rpc-port 7000
  worker: ./bin/beyondtp staff --host localhost --manager localhost:7000
